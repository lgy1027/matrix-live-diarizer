# Web 框架
fastapi
uvicorn>=0.23.0
python-multipart>=0.0.6

# 深度学习
# 锁住 torch/torchvision/torchaudio 配套版本 — 不同步会 RuntimeError
# (torchvision::nms does not exist 等) — 必须三方一致
torch>=2.0.0
torchvision
torchaudio
transformers>=4.30.0

# transformers 间接依赖(隐式,不写会报 import error)
huggingface-hub
tokenizers
safetensors
accelerate
packaging
pyyaml

# ASR 模型
modelscope>=1.10.0
qwen-asr>=0.0.6

# 声纹向量库
chromadb>=0.4.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# 数据模型
pydantic>=2.0.0

# 配置管理
python-dotenv>=1.0.0

# modelscope 隐式依赖
setuptools<81              # pkg_resources 在 81+ 版本移除
addict
simplejson
sortedcontainers

# soundfile 隐式依赖
cffi