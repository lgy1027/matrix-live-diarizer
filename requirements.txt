# Web 框架
fastapi
uvicorn>=0.23.0
python-multipart>=0.0.6

# 深度学习
# 锁住 torch/torchvision/torchaudio 配套版本 — 不同步会 RuntimeError
# (torchvision::nms does not exist 等) — 必须三方一致
torch>=2.0.0
torchvision
torchaudio
transformers>=4.30.0

# transformers 间接依赖(隐式,不写会报 import error)
huggingface-hub
tokenizers
safetensors
accelerate
packaging
pyyaml

# ASR 模型
modelscope>=1.10.0
qwen-asr>=0.0.6

# 声纹向量库
chromadb>=0.4.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# 数据模型
pydantic>=2.0.0

# 鉴权
PyJWT>=2.8.0
werkzeug>=3.0.0

# 配置管理
python-dotenv>=1.0.0

# modelscope 隐式依赖
setuptools<81              # pkg_resources 在 81+ 版本移除
addict
simplejson
sortedcontainers

# soundfile 隐式依赖
cffi

# 摘要 (TextRank 兜底) — LLM 不可用时本地提取
summa==1.2.0

# 离线高准确度说话人识别 (pyannote 3.1)
# 用于: 上传文件 ?diarization=pyannote (Diarization Error Rate ~18%)
# 需要:
#   1. brew install ffmpeg (macOS) 或 apt install ffmpeg (Linux)
#   2. HF_TOKEN 环境变量 + https://huggingface.co/pyannote/segmentation-3.0 接受用户条款
# 详细: docs/SPEAKER_DIARIZATION.md
pyannote.audio>=3.1.0
