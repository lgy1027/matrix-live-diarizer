[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "timeout(seconds): fail a test if it runs longer than the given number of seconds",
    "asyncio: mark test as async (handled by pytest-asyncio)",
]
